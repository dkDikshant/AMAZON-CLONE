import React from 'react';
import { Box, styled } from '@mui/material';
import './Slides.css'

const BoxStyle = styled(Box)({
    width: '20%',
    height: '150px',
    background: 'red'

})
const SlidesBoxes = ({ caption }) => {
    return (
        <div>
            <BoxStyle>
                zdbnjl
            </BoxStyle>
        </div>
    )
}

export default SlidesBoxes
